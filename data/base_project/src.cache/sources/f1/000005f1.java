package android.hardware;

import android.os.Binder;
import android.os.IBinder;
import android.os.IInterface;
import android.os.Parcel;
import android.os.RemoteException;

/* loaded from: IProCameraCallbacks.class */
public interface IProCameraCallbacks extends IInterface {

    /* loaded from: IProCameraCallbacks$Stub.class */
    public static abstract class Stub extends Binder implements IProCameraCallbacks {
        private static final String DESCRIPTOR = "android.hardware.IProCameraCallbacks";

        public Stub() {
            attachInterface(this, DESCRIPTOR);
        }

        public static IProCameraCallbacks asInterface(IBinder obj) {
            if (obj == null) {
                return null;
            }
            IInterface iin = obj.queryLocalInterface(DESCRIPTOR);
            if (iin != null && (iin instanceof IProCameraCallbacks)) {
                return (IProCameraCallbacks) iin;
            }
            return new Proxy(obj);
        }

        @Override // android.os.IInterface
        public IBinder asBinder() {
            return this;
        }

        @Override // android.os.Binder
        public boolean onTransact(int code, Parcel data, Parcel reply, int flags) throws RemoteException {
            switch (code) {
                case IBinder.INTERFACE_TRANSACTION /* 1598968902 */:
                    reply.writeString(DESCRIPTOR);
                    return true;
                default:
                    return super.onTransact(code, data, reply, flags);
            }
        }

        /* loaded from: IProCameraCallbacks$Stub$Proxy.class */
        private static class Proxy implements IProCameraCallbacks {
            private IBinder mRemote;

            Proxy(IBinder remote) {
                this.mRemote = remote;
            }

            @Override // android.os.IInterface
            public IBinder asBinder() {
                return this.mRemote;
            }

            public String getInterfaceDescriptor() {
                return Stub.DESCRIPTOR;
            }
        }
    }
}